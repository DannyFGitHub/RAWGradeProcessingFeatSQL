package enums;

/**
 * Enum for storing the grades value names, changing it here will change it in the DatabaseEntry object calculation.
 */
public enum Grades {
    HD,
    DI,
    CR,
    PS,
    FL
}
